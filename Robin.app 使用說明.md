# Robin.app 使用說明

原生 Mac app,不開瀏覽器、不跑 localhost。介面與功能同前:貼網址 → 解析 → 選格式(影片 MP4/MKV/WebM + 畫質;音訊 MP3/M4A/WAV/FLAC/Opus)→ 下載,螢光綠進度填滿動畫,完成直接存到 `~/Downloads`。

## 第一次開啟

1. 確認已裝 ffmpeg:`brew install ffmpeg`
2. 對 Robin.app 按右鍵 →「打開」(未簽名 app,macOS 會擋直接雙擊,只需這樣做一次)
3. 首次啟動會自動安裝元件(約一分鐘,會有通知),之後啟動秒開

## 提醒

- 之後正常雙擊即可開啟
- 可拖到 Dock 或「應用程式」資料夾
- 舊的 `Works_Dev/Robin`(網頁版)可以刪掉了
- 請只下載你有權利下載的內容
